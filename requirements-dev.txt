# Lint/format/test tooling -- not installed in the Docker image (Dockerfile
# installs requirements.txt only). Used for local dev and CI.
-r requirements.txt

black==26.5.1
iniconfig==2.3.0
mypy_extensions==1.1.0
pathspec==1.1.1
platformdirs==4.11.3
pluggy==1.6.0
Pygments==2.20.0
pytest==9.1.1
pytest-django==4.14.0
# Parallel test execution. The suite is ~39 minutes serially on 8 cores
# and ~12 with `pytest -n 8`, which is the difference between a usable
# pre-commit gate and one people skip.
pytest-xdist==3.8.0
pytokens==0.4.1
ruff==0.16.3
